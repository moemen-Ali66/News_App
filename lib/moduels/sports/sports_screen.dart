import 'package:flutter/material.dart';

class SportsScrren extends StatelessWidget {
  const SportsScrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('sports screen',style: TextStyle(fontSize: 20,fontWeight: FontWeight.bold),),
    );
  }
}
