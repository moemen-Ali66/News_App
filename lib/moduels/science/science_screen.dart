import 'package:flutter/material.dart';

class ScienceScrren extends StatelessWidget {
  const ScienceScrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('science screen',style: TextStyle(fontSize: 20,fontWeight: FontWeight.bold),),
    );
  }
}
